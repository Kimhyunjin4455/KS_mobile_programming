<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobilefinal00">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileFinal00">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SecondActivity" android:label="Second 액티비티"/>
        <activity android:name=".ExplaActivity" android:label="Expla 액티비티"/>
        <activity android:name=".DetailActivity" android:label="Detail 액티비티"/>
        <activity android:name=".PopMovieActivity" android:label="Popular 액티비티"/>
        <activity android:name=".AdmissionActivity" android:label="Admission 액티비티"/>
        <activity android:name=".AddInfoActivity" android:label="AddInfo 액티비티"/>
        <activity android:name=".MyInfoActivity" android:label="MyInfo액티비티"/>

    </application>

</manifest>